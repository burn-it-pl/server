import router from "../src/app/routes/app";

// const port = process.env.PORT || 3000;
const app = router;

// app.listen(port, () => {
//   console.log("APP_LISTENING_ON_PORT", { port });
// });

export default app;